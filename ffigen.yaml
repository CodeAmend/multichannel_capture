# Run with `dart run ffigen --config ffigen.yaml`.
name: MultichannelCaptureBindings
description: |
  Bindings for `src/multichannel_capture.h`.

  Regenerate bindings with `dart run ffigen --config ffigen.yaml`.
output: 'lib/multichannel_capture_bindings_generated.dart'
headers:
  entry-points:
    - 'src/multichannel_capture.h'
  include-directives:
    - 'src/multichannel_capture.h'
comments:
  style: any
  length: full
